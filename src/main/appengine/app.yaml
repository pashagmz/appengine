runtime: java11
env: standard
runtime_config:
  jdk: openjdk11
env_variables:
  ACTIVE_PROFILE: "production"
handlers:
  - url: /.*
    script: this field is required, but ignored
manual_scaling:
  instances: 1